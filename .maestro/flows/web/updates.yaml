# Web entry for the updates steps (subflows/steps/updates.yaml). Keep it to these two runFlow lines.
name: web/updates
url: ${APP_URL}
tags: [web]
---
- runFlow: ../../subflows/launch-web.yaml
- runFlow: ../../subflows/steps/updates.yaml

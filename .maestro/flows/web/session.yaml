# Web entry for the session steps (subflows/steps/session.yaml). Keep it to these two runFlow lines.
name: web/session
url: ${APP_URL}
tags: [web]
---
- runFlow: ../../subflows/launch-web.yaml
- runFlow: ../../subflows/steps/session.yaml

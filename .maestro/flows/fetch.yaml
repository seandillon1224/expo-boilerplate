# Native entry for the fetch steps (subflows/steps/fetch.yaml). Keep it to these two runFlow lines.
name: native/fetch
appId: ${APP_ID}
tags: [ios, android]
---
- runFlow: ../subflows/launch.yaml
- runFlow: ../subflows/steps/fetch.yaml

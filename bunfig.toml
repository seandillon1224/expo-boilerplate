[install]
# Commit `bun.lock` (text) rather than the binary `bun.lockb`, so lockfile diffs are reviewable
# and Renovate/`gh` can read them. CI installs with --frozen-lockfile.
saveTextLockfile = true

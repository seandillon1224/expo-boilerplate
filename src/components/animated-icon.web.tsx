// The native splash overlay animates the app icon while expo-splash-screen hides;
// web has no native splash, so there is nothing to overlay.
export function AnimatedSplashOverlay() {
  return null;
}

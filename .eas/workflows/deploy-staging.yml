# Staging rung of the delivery ladder (PLAN.md decisions 3, 9, 10, 12, 13). Every push to main:
# fingerprint → get_build (staging profile, both platforms; miss = paid internal build) → update
# to the `staging` channel (Sentry maps best-effort) → deploy the web export to the EAS Hosting
# `staging` alias → Slack with install links, flagged "reinstall required" when a build was cut.
# Same cache idiom as e2e.yml; the runbook is docs/release-ladder.md.
#
# Run by hand:  bun run eas workflow:run .eas/workflows/deploy-staging.yml [-F hosting=enabled] [-F critical=yes]
# Validate:     bun run eas workflow:validate .eas/workflows/deploy-staging.yml   (cap: 16 KiB)
# Push triggers need the Expo GitHub App linked to the repo (docs/release-ladder.md → Enable).
#
# Two repo constants live in this file, each as a `workflow_dispatch` input whose `default`
# equals the `|| '<literal>'` fallback on the job `if` (inputs are empty outside a dispatch):
#   IOS_BUILDS  build_ios.if    disabled until the iOS ad hoc credentials exist (owner runbook)
#   HOSTING     deploy_web.if   disabled until the owner's first manual `eas deploy` (dev-domain)
# Flip = change the literal AND the input default, in one PR. Until then the job is skipped. Slack
# needs no constant: the job skips itself while SLACK_WEBHOOK_URL is unset on EAS. So the run
# stays green with none of the owner-owed secrets in place.
name: Deploy staging

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      ios_builds:
        type: choice
        options: [enabled, disabled]
        default: disabled
        required: false
        description: enabled = cut an iOS staging build on a fingerprint miss (needs ad hoc credentials); disabled = skip it, Android + update + web still run.
      hosting:
        type: choice
        options: [enabled, disabled]
        default: disabled
        required: false
        description: enabled = deploy the web export to the EAS Hosting `staging` alias.
      critical:
        type: choice
        options: ['yes', 'no']
        default: 'no'
        required: false
        description: yes = publish a critical (forced) update (ADR-0003) — installed apps download and reload it at once; promotion carries the flag along. Verify on staging before promoting.

concurrency:
  # EAS never queues runs; without this two pushes publish in parallel and the OLDER one can land
  # on `staging` last. Cancelling costs at most one partial build (the next run rebuilds it);
  # `eas update` / `eas deploy` are atomic server-side, so a cancelled publish leaves nothing.
  cancel_in_progress: true
  group: ${{ workflow.filename }}-${{ github.ref }}

jobs:
  fingerprint:
    name: Fingerprint
    type: fingerprint
    # Must equal the `staging` build profile (eas.json → environment + env), or the hash never
    # matches a build and the update never reaches one (runtimeVersion = fingerprint).
    environment: preview
    env:
      APP_VARIANT: staging

  # --- iOS ---------------------------------------------------------------------------------

  get_build_ios:
    name: Get build (ios)
    type: get-build
    needs: [fingerprint]
    params:
      platform: ios
      profile: staging
      distribution: internal
      fingerprint_hash: ${{ needs.fingerprint.outputs.ios_fingerprint_hash }}
      # Two runs racing on the same new fingerprint: the second waits for the first's build.
      wait_for_in_progress: true

  build_ios:
    name: Build (ios)
    type: build
    needs: [get_build_ios]
    # IOS_BUILDS — needs the ad hoc credentials from docs/environments-and-secrets.md → iOS
    # runbook; until then every attempt fails at "Credentials are not set up".
    if: ${{ !needs.get_build_ios.outputs.build_id && (inputs.ios_builds || 'disabled') == 'enabled' }}
    params:
      platform: ios
      profile: staging
      message: staging build for fingerprint ${{ needs.fingerprint.outputs.ios_fingerprint_hash }}

  # --- Android -----------------------------------------------------------------------------

  get_build_android:
    name: Get build (android)
    type: get-build
    needs: [fingerprint]
    params:
      platform: android
      profile: staging
      distribution: internal
      fingerprint_hash: ${{ needs.fingerprint.outputs.android_fingerprint_hash }}
      wait_for_in_progress: true

  build_android:
    name: Build (android)
    type: build
    needs: [get_build_android]
    if: ${{ !needs.get_build_android.outputs.build_id }}
    params:
      platform: android
      profile: staging
      message: staging build for fingerprint ${{ needs.fingerprint.outputs.android_fingerprint_hash }}

  # --- OTA update --------------------------------------------------------------------------

  update:
    name: Update (staging)
    type: update
    # `after`, not `needs`: a failed or skipped build must not block the OTA — the update is
    # keyed by fingerprint, so it is harmless for a platform with no matching build and still
    # reaches every other installed staging build. Only a failed fingerprint stops it.
    after: [fingerprint, get_build_ios, build_ios, get_build_android, build_android]
    if: ${{ after.fingerprint.status == 'success' }}
    # Same environment + APP_VARIANT as the profile, so the exported runtime version (fingerprint)
    # equals the staging builds' and EXPO_PUBLIC_* resolve to the preview values.
    environment: preview
    env:
      APP_VARIANT: staging
      # `critical` input (dispatch only; a push is never critical): app.config.ts writes
      # `extra.updatePolicy: 'forced'` into the manifest. Never an EAS env var — that would make every
      # update critical — and not `EXPO_PUBLIC_`, so it stays out of the bundle. Outside the fingerprint
      # (`ExpoConfigExtraSection` skipped in fingerprint.config.js), so the runtime version is unchanged.
      EAS_UPDATE_CRITICAL: "${{ inputs.critical == 'yes' ? '1' : '0' }}"
    params:
      channel: staging
      # Dispatch runs have no commit message; keep the update findable either way.
      message: "${{ (inputs.critical == 'yes' ? 'CRITICAL: ' : '') + (github.commit_message || 'manual staging publish of ' + (github.sha || 'HEAD')) }}"
      # Sentry source maps are uploaded by the job itself, from the same export it publishes
      # (`dist` only exists on this worker, so a separate job cannot do it). Unset = best-effort:
      # `@sentry/react-native` is installed so it tries, and without SENTRY_AUTH_TOKEN /
      # SENTRY_ORG / SENTRY_PROJECT on EAS it logs the error and stays green. Once the owner
      # sets them (docs/environments-and-secrets.md → Human setup checklist) flip this to `true`
      # so a broken upload fails the run instead of silently shipping unsymbolicated errors.
      # upload_sentry_sourcemaps: true

  # --- Observe (informational) --------------------------------------------------------------

  observe:
    name: Observe TTI (last 7 days)
    # Prints the startup-TTI state of what staging has been running against observe-budget.json
    # (docs/observe.md). Observe samples arrive after installs launch AND background, so this
    # reads the soak of the PREVIOUS group(s), never the update just published — the on-demand
    # / cron check is .eas/workflows/observe-check.yml. Never fails the run: a breach is logged
    # (and the exit code swallowed) so the staging rung stays green; the gate lives in promote.
    needs: [update]
    environment: preview
    steps:
      - uses: eas/checkout
      - uses: eas/install_node_modules
      - name: bun run observe:check
        run: |
          export EXPO_TOKEN="${ eas.job.secrets.robotAccessToken }"
          bun run observe:check --days 7 || echo "observe-check: over budget — informational here; see docs/observe.md → Gating on TTI."

  # --- Web ---------------------------------------------------------------------------------

  deploy_web:
    name: Deploy web (staging)
    type: deploy
    # The job runs `expo export -p web` itself, then `eas deploy` — no prior export job needed.
    needs: [update]
    # HOSTING — the first deployment of a project claims its dev-domain (interactive, paid):
    # docs/release-ladder.md → Enable. Runs after the update so web never leads native.
    if: ${{ (inputs.hosting || 'disabled') == 'enabled' }}
    environment: preview
    env:
      APP_VARIANT: staging
    params:
      # Preview deployment (never `prod`) promoted to the stable `staging` alias; T5.2 moves the
      # `uat` / `production` aliases the same way.
      alias: staging
      prod: false

  # --- Slack -------------------------------------------------------------------------------

  slack:
    name: Slack
    # A custom job, not `type: slack`: eas-cli 23's validator rejects any `${{ }}` expression in
    # that job's `webhook_url` (schema `format: uri`), so the URL would have to be a literal —
    # and a webhook is a secret. The `eas/send_slack_message` STEP does take an expression, so the
    # job composes the text, then a guard step reads SLACK_WEBHOOK_URL (secret, preview
    # environment) from the VM env and the send step is skipped while it is unset; nothing to flip
    # once the owner sets it. Unverified until the webhook exists (the step ran in no run yet).
    # `after` on everything so the post goes out on a red run too; `after.<job>.status` is
    # success | failure | skipped, and a skipped job has no outputs (so every value can be empty).
    # The wording lives in scripts/eas/slack-compose.js, which reads this env block; `set-output`
    # base64-encodes the value, so the multi-line text survives as a step output.
    after:
      [fingerprint, get_build_ios, build_ios, get_build_android, build_android, update, deploy_web]
    environment: preview
    env:
      WORKFLOW_URL: ${{ workflow.url }}
      SHA: ${{ github.sha }}
      COMMIT_MESSAGE: ${{ github.commit_message }}
      CRITICAL: ${{ inputs.critical }}
      UPDATE_STATUS: ${{ after.update.status }}
      UPDATE_GROUP_ID: ${{ after.update.outputs.first_update_group_id }}
      IOS_FP: ${{ after.fingerprint.outputs.ios_fingerprint_hash }}
      ANDROID_FP: ${{ after.fingerprint.outputs.android_fingerprint_hash }}
      BUILD_IOS_STATUS: ${{ after.build_ios.status }}
      BUILD_IOS_ID: ${{ after.build_ios.outputs.build_id }}
      GET_BUILD_IOS_ID: ${{ after.get_build_ios.outputs.build_id }}
      BUILD_ANDROID_STATUS: ${{ after.build_android.status }}
      BUILD_ANDROID_ID: ${{ after.build_android.outputs.build_id }}
      GET_BUILD_ANDROID_ID: ${{ after.get_build_android.outputs.build_id }}
      DEPLOY_WEB_STATUS: ${{ after.deploy_web.status }}
      DEPLOY_URL: ${{ after.deploy_web.outputs.deploy_url }}
    steps:
      - uses: eas/checkout
      - id: compose
        name: Compose message
        run: set-output text "$(node scripts/eas/slack-compose.js staging)"
      - id: guard
        name: Skip without webhook
        run: |
          if [ -n "${SLACK_WEBHOOK_URL:-}" ]; then set-output has_slack true
          else echo "SLACK_WEBHOOK_URL is not set in the preview environment — skipping (docs/release-ladder.md → Enable)."; set-output has_slack false; fi
      - uses: eas/send_slack_message
        # The secret never appears in the log: it goes in as an expression, not a shell argument.
        if: ${{ steps.guard.outputs.has_slack == 'true' }}
        with:
          message: ${{ steps.compose.outputs.text }}
          slack_hook_url: ${{ env.SLACK_WEBHOOK_URL }}

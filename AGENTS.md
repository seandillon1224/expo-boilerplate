# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

<!--
The major in that URL must match the `expo` dependency in package.json; when the SDK is upgraded,
bump it here too. `scripts/__tests__/expo-docs-version.test.ts` fails the gate if they disagree.
-->
